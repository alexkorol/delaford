/**
 * Static overworld monster definitions.
 *
 * Empty since the Crossroads redesign (docs/crossroads-world-web.md): the
 * town is truce-ground — no blade drawn where the roads cross — and every
 * fight now lives in the generated world-web zones. The old trio (Ashen
 * Wolf, Hollow Guard, Ember Seer) spawned into the default town scene and
 * killed idle scions on the plaza.
 */
export default [];
